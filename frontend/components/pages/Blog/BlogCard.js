import React from 'react'
import PropTypes from 'prop-types'

const BlogCard = ({ data }) => {
  return (
    <div>
      <p>mans</p>
    </div>
  )
}

BlogCard.propTypes = {
  data: PropTypes.object
}

export default BlogCard
